# 2026-09-11 / DATA-OFFER2 legacy Offer provenance guard

- Task type: execution / Medium read-path authority hardening. DB schema, migration, RLS, Product mutation, Offer mutation, recommendation ranking, and provider execution are excluded.
- Baseline: fresh main `62ca540d22fa457bb064eca6e19fb12f1338495c`. Production read-only inspection found 5 `legacy_product_buy_link_v1` Offer rows, all Olive Young, all `product_subject_unresolved`; Product Identity bindings did not independently establish the Olive Young seller listing identity for any row. One separate Product Evidence subject binding was `exact_subject_match/equivalent`, but it did not bind the Olive Young listing itself.
- Circularity finding: DATA-SRC6 constructs legacy Offer listing identity from the existing `products.buy_link` and intentionally inserts `price_amount=null`, `availability_state=unknown`, `product_scope_state=product_subject_unresolved`, and null observation timestamps. Therefore that legacy source cannot self-authorize a later `product` scope or current price.
- Implementation: `product-offer-read-v2` replaces the legacy source allowlist with an explicit source policy. `legacy_product_buy_link_v1::oliveyoung` is admitted only for `product_subject_unresolved`, remains link-only, and has `priceAuthority=false`. A legacy row changed to `product` scope fails closed to the existing Naver fallback. Populated legacy price/timestamp fields still cannot override Product legacy price presentation. Unregistered future source names also fail closed until explicitly reviewed and added.
- Verification contract: focused verifier now covers link-only positive behavior, legacy price non-authority even when in-stock/observed, circular `product` scope rejection, deterministic selection with a rejected stronger-scope row, unregistered source rejection, ranking/score/price invariance, existing malformed/market/lifecycle checks, and Offer-read failure fallback.
- Production impact: read-only queries only; no hosted data write, no schema/migration/RLS change, no Product/Offer update/delete/insert, and no recommendation score or order change.
- Completion gate: log-bearing exact head must pass the Offer read-path workflow and normal affected PR checks before merge. Fresh main must be re-read immediately before any expected-head squash merge.
- Next boundary: define a separately provenance-bearing seller observation source contract for independently observed listing identity, price, availability, and observation timestamps; do not reuse `legacy_product_buy_link_v1` for that authority.
- Context promotion candidate: none.
