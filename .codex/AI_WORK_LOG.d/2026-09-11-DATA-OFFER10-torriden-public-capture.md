# 2026-09-11 / DATA-OFFER10 Torriden public capture

- Task type: execution / legitimate alternate seller-source acquisition after DATA-OFFER9 Olive Young hosted HTTP 403 boundary.
- Initial fresh baseline: `223ee33b5f68992c4f858b7ddcd8ac2aa07decf7` after TRUST-P12 #430.
- Existing repository authority: `crawler/config/offer-source-rules.json` already classifies `torriden.com` as seller key `torriden_official` and admits `/goods/goods_view.php?goodsNo=...` with listing id sourced from `goodsNo`.
- Candidate: official Torriden product detail `https://www.torriden.com/goods/goods_view.php?goodsNo=136`, publicly indexed as `다이브인 무기자차 마일드 선크림 60ml`. External search observations are discovery context only and are not imported as parser or price authority.
- Purpose: test whether ordinary unauthenticated hosted GET can acquire independently reviewable source bytes for an already-authorized seller route.
- Network policy: exact HTTPS target; host exactly `www.torriden.com`; path exactly `/goods/goods_view.php`; goodsNo exactly `136`; GET only; no Authorization header; no Cookie header; descriptive BEJEWELY user-agent; manual same-allowlist redirects only, maximum 3; 20 second timeout; successful source capture requires 2xx HTML/XHTML; response body maximum 5 MiB.
- Success evidence: DATA-OFFER6 `seller-listing-capture-evidence-v1` followed by DATA-OFFER8 `seller_listing_parser_fixture_v1` with provenance class `captured_source`; response bytes retained as canonical base64 only inside the Actions artifact fixture.
- Access-boundary evidence: exact hosted HTTP 403 remains fail-closed and is represented as metadata-only `source_access_blocked` with `capture_admitted=false`; no parser fixture may exist. Other failures remain errors.
- Deterministic verification: exact target pinning, no auth/cookies, same-allowlist redirect handling, cross-origin rejection, HTTP/content-type/body-size/empty-body failures, transport failure wrapping, exact 403 classification, DATA-OFFER6 integrity evidence, DATA-OFFER8 captured-source admission.
- Artifact verifier: independently validates captured fixture bytes/digest/listing URL or the exact metadata-only 403 non-admission shape.
- Hosted evidence on initial exact head `21fade459e5e39b8022241fe4cd3b692a9acbf2e`: DATA-OFFER10 workflow run `34534268665`, job `103062095233` completed SUCCESS. The unauthenticated canonical GET returned HTTP 200 with no redirect and outcome `captured_source`.
- Captured payload evidence: content type `text/html; charset=UTF-8`, byte length `175733`, payload SHA-256 `998b5227642ec34cb2e7eab03eee3167a06219def3890ce0267c1a71eb67e4e7`, and the bytes contain the pinned goodsNo `136`.
- Hosted artifact: ID `10174754052`, name `data-offer10-torriden-public-capture-21fade459e5e39b8022241fe4cd3b692a9acbf2e`, ZIP SHA-256 `0150f287a86b6c18ca13fe6c89ce4152a6618e247a41ae4837a07401944eedcf`, size `58992` bytes, expires `2026-09-24T21:49:59Z`. Direct artifact inspection confirmed two files: capture metadata and a captured-source parser fixture. The fixture decodes to exactly `175733` bytes and reproduces the payload SHA-256 above.
- Direct source review findings for the captured payload: listing identity is redundantly represented by page-level `goodsNo` and controller `setGoodsNo`, both `136`; sale price is redundantly represented by controller price, hidden sale-price input, and analytics value, all numerically `17500`; currency is explicitly represented as `KRW`; the current page also contains active buy/cart controls, Naver Pay enabled state, and unbounded-stock signals. No source-specific parser existed before this review.
- Parser design boundary established by review: a future Torriden parser may return `listing_id=136` only when redundant listing-id signals agree; may return a KRW price only when independent price signals and explicit currency agree; may classify `in_stock` only when positive purchase-enable and stock signals agree. Missing/conflicting signals fail closed to null price or `unknown` availability. `out_of_stock` must not be invented without a directly reviewed sold-out source fixture.
- Authority boundary: source bytes establish only seller response provenance and integrity. Parser extraction, even when later implemented, does not establish Product binding, Product Fact Subject binding, Offer materialization, recommendation authority, or cross-product identity.
- DATA-OFFER10 itself does not implement the Torriden parser. DATA-OFFER11 may implement it only from these directly reviewed source semantics and must separately verify against live/captured evidence.
- Production Supabase query/write: zero. Retailer writes: zero. Product/Offer writes: zero. Access-control bypass: zero.
- Merge-gate rule: the `21fade45...` run is hosted evidence but becomes stale if the PR head or base changes. Final merge requires a fresh single-commit head on current main and exact-head CI/live capture rerun; stale checks are never reused.
