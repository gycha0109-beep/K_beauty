# 2026-09-11 / DATA-OFFER11 Torriden captured-source parser

- Task type: execution / retailer-specific parsing after DATA-OFFER10 acquired and directly reviewed real hosted source bytes.
- Initial baseline: DATA-OFFER10 merged main `0e8d1abb77949d6e7fcc68d1dbbc03f81914bb18`.
- Seller authority remains `torriden_official`; canonical listing URL remains `https://www.torriden.com/goods/goods_view.php?goodsNo=136`; source version remains `torriden-public-product-html-v1`.
- DATA-OFFER10 merged-main capture reproduced HTTP 200 `captured_source`, 175733 bytes, proving hosted unauthenticated source acquisition remains available. DATA-OFFER11 consumes only DATA-OFFER8-admitted `captured_source` fixtures and revalidates seller/listing/source/content-type authority before parsing.
- Implementation live run `34543542957` exposed that the current hosted page contains two page-level `var goodsNo = '136'` declarations. Direct inspection of the same-head DATA-OFFER10 artifact `10178188365` confirmed both values are exactly `136`, while controller `setGoodsNo`, price signals, and currency signals each remain present and consistent.
- Repeated-signal policy: a signal family may occur one or more times, but every observed value inside that family must normalize to the same value. Missing families fail closed; conflicting repeated values fail closed. The parser never selects the first occurrence to resolve a conflict.
- Listing identity admission: page-level `var goodsNo = '...'` family and controller `setGoodsNo` family must both be present, each family must internally agree, both families must agree with each other, and the final value must equal the pinned canonical URL `goodsNo`.
- Price admission: controller `setGoodsPrice` and hidden `set_goods_price` families must both be present and internally consistent, parse as finite nonnegative numbers, and agree numerically. Currency `gdCurrencyCode` and `fx.base` families must both be present and internally consistent, agree with each other, and equal `KRW`.
- Availability admission is intentionally one-way and fail-closed. `in_stock` is emitted only when all reviewed positive signals coexist: active `btn_add_order` buy button, active `btn_add_cart` cart button, NaverPay `ENABLE: "Y"`, controller `setStockFl: 'n'` + `setStockCnt: '∞'`, and hidden `set_goods_stock=∞`. Missing or conflicting positive signals yield `unknown`.
- `out_of_stock` is not emitted in v1. No directly reviewed sold-out Torriden fixture exists, so inverse inference is prohibited until separate captured-source evidence is acquired and reviewed.
- Parser output is exactly DATA-OFFER5 parser-owned fields: `listing_id`, `price`, `availability`. It contains no seller override, listing URL override, observed timestamp override, source-version override, Product ID, Product Fact Subject ID, Offer ID, or recommendation authority.
- Hosted DATA-OFFER11 CI performs a new bounded DATA-OFFER10 capture, revalidates the capture artifact, parses the captured fixture, and passes the composed result through the DATA-OFFER3 observation contract without persistence.
- Full retailer HTML is not checked into the repository. Hosted raw bytes remain temporary Actions artifact evidence only.
- Production Supabase query/write: zero. Seller observation persistence: zero. Product binding: zero. Offer materialization: zero. Recommendation authority: zero. Retailer writes: zero.
- Next boundary after parser validation: a separately governed Product/source binding decision, followed only later by Offer materialization. Parsed seller values do not self-acquire either authority.
