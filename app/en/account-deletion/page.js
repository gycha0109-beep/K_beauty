import AccountDeletionPanel from "@/components/account/AccountDeletionPanel";

export const metadata = {
  title: "Delete account | BEJEWELY",
  description: "Official BEJEWELY web resource for deleting an account and associated consumer data."
};

export default function AccountDeletionPage() {
  return <AccountDeletionPanel locale="en" />;
}
