import SupportPanel from "@/components/support/SupportPanel";

export const metadata = {
  title: "Support | BEJEWELY",
  description: "Official BEJEWELY support for account access, skin analysis, saved reports, the skin diary, and privacy-related requests."
};

export default function SupportPage() {
  return <SupportPanel locale="en" />;
}
