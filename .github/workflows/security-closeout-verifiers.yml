name: EVAL-R1 Executor Registration Smoke

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo EVAL_R1_EXECUTOR_REGISTRATION_OK
