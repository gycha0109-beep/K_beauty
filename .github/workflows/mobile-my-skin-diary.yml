name: BEJEWELY Mobile My Skin Diary

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/mobile/**"
      - "app/api/my/dashboard/route.js"
      - "app/api/my/check-in/route.js"
      - "app/api/my/diary-day/route.js"
      - "lib/my/dashboard.js"
      - "lib/my/diary-day.js"
      - "lib/my/routine-generator.js"
      - "lib/supabase/server-client.js"
      - "scripts/verify-mobile-my-skin-diary.mjs"
      - ".github/workflows/mobile-my-skin-diary.yml"
  push:
    branches: [main]
    paths:
      - "apps/mobile/**"
      - "app/api/my/dashboard/route.js"
      - "app/api/my/check-in/route.js"
      - "app/api/my/diary-day/route.js"
      - "lib/my/dashboard.js"
      - "lib/my/diary-day.js"
      - "lib/my/routine-generator.js"
      - "lib/supabase/server-client.js"
      - "scripts/verify-mobile-my-skin-diary.mjs"
      - ".github/workflows/mobile-my-skin-diary.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mobile-my-skin-diary-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  mobile-my-skin-diary:
    name: BEJEWELY Mobile My Skin Diary
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout exact candidate SHA
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Attest exact checked-out SHA
        shell: bash
        run: |
          set -euo pipefail
          expected="${{ github.event.pull_request.head.sha || github.sha }}"
          actual="$(git rev-parse HEAD)"
          test "$actual" = "$expected"
          printf 'MOBILE_MY_SKIN_DIARY_SHA=%s\n' "$actual"

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install monorepo dependencies
        run: npm ci

      - name: Verify MOBILE-3 My / Skin Diary boundaries
        run: node scripts/verify-mobile-my-skin-diary.mjs

      - name: Run MOBILE-2 auth regression verifier
        run: node scripts/verify-mobile-auth-foundation.mjs

      - name: Run mobile TypeScript check
        run: npm run mobile:typecheck

      - name: Validate Expo public config
        run: npm run mobile:config

      - name: Export Android JS bundle
        run: npm run mobile:export:android
