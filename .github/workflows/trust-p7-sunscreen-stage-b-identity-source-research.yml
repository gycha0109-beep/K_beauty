name: TRUST-P7 Sunscreen Stage B Identity Source Research

on:
  pull_request:
    paths:
      - "evidence/product-fact-subject-coverage-v1/trust-p7-sunscreen-stage-b-identity-source-research-v1.json"
      - "scripts/product-evidence/verify-trust-p7-sunscreen-stage-b-identity-source-research-v1.mjs"
      - "docs/evidence/trust-p7-sunscreen-stage-b-identity-source-research-v1.md"
      - ".github/workflows/trust-p7-sunscreen-stage-b-identity-source-research.yml"
  push:
    branches:
      - main
    paths:
      - "evidence/product-fact-subject-coverage-v1/trust-p7-sunscreen-stage-b-identity-source-research-v1.json"
      - "scripts/product-evidence/verify-trust-p7-sunscreen-stage-b-identity-source-research-v1.mjs"
      - "docs/evidence/trust-p7-sunscreen-stage-b-identity-source-research-v1.md"
      - ".github/workflows/trust-p7-sunscreen-stage-b-identity-source-research.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-trust-p7-sunscreen-stage-b-research:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1

      - name: Attest exact head
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify frozen TRUST-P7 Stage B authority research
        run: node scripts/product-evidence/verify-trust-p7-sunscreen-stage-b-identity-source-research-v1.mjs
