# V2.1-8N Exfoliation Load Calibration Feasibility Reassessment

Execution main: `a2536bac8def3b28dc71e02c0c09099c299633b1`. Hosted remains read-only. V2.1-8M is preserved as `NO_NUMERIC_ANCHOR_SOURCE_FOUND` with numeric=0 and authoritative ordinal=0 for its exact governed scope.

## Downstream audit

15 requirements are frozen: IMPLEMENTED 12 / DESIGNED 1 / NOT_ESTABLISHED 2. Calibrated numeric magnitude required=0. Production, shadow, routine, current-product and CandidatePolicy paths use signal/identity/overlap/context/caution or legacy count proxies; V2.1-7 keeps governed PDA numeric contribution `null`.

## Semantics

Active identity != potency. Concentration != comparable biological response. Frequency/format != efficacy. Routine overlap != intrinsic product potency. User sensitivity/reaction history != product property. The 8L measured final-product response is a separate semantic family.

## Path decision

Numeric calibration is not primary: downstream numeric necessity, admitted anchors and catalog-scale acquisition feasibility are not established. Axis redesign has real semantic pressure but is not mandatory because current PDA/user/routine layers already separate the concepts. A structured non-numeric PDA can preserve unordered identity, context, missingness, uncertainty and coverage without potency ordering.

Primary terminal outcome: `NON_NUMERIC_DECISION_REPRESENTATION_RECOMMENDED`.

This does not activate production, create ordinal potency, publish `exfoliation_response_change`, change scorer/ranker/CandidatePolicy, or invalidate 8M. Production invariance remains 164 x 12 = 1968 evaluations with all required deltas zero.

Exactly one next-stage recommendation: **Exfoliation Non-Numeric Decision Axis Contract Design**. It freezes categorical/structural output, missingness, authority, multi-value semantics and shadow behavior; no production activation.
